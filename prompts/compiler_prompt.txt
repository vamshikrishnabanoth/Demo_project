Write a Compiler Error multiple-choice question.
The stem MUST present a code snippet containing a syntax violation, type mismatch, scoping issue, or package import failure.
Ask the student to identify why the code will fail compilation or what compile-time error will be thrown.
Provide the faulty code block inside the 'code_snippet' field (wrapped in markdown gates, e.g. ```{programming_language} ... ```).
Target Concept: {concept_tag}
Difficulty: {difficulty}
Context chunks: {context}
Programming Language: {programming_language}
